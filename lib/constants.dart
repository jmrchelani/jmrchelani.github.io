import 'package:flutter/material.dart';

const kDefaultPadding = 16.0;
const kDefaultDuration = Duration(seconds: 1); // we use it on our animation
const kMaxWidth = 1024.0; // max width of our web

const kWhiteColor = Color(0xFFFFFFFF);
const kBlackColor = Color(0xFF000000);
const kBlueColor = Color(0xFF0E1630);
const kGreyColor = Color(0xFF808DAD);
const kRedColor = Color(0xFFEB4A4A);
